# Python dependencies for this repo's tooling.
#   pip install -r requirements.txt   (or: make deps)
#
# verify-initiative.py needs PyYAML; byte-integrity.py needs pefile (XIP un-rebase).
# The other reconcile/interop scripts are stdlib-only.
PyYAML>=6.0.3
pefile>=2024.8.26   # byte-integrity.py — PE parsing for XIP/PEI un-rebase (R4 phase 3)

# External CLI tools the lanes shell out to are NOT pip-installable — install
# them via your package manager / release binaries:
#   opa      (policy gate) — pinned; run `make bin` (scripts/fetch-tools.sh) to fetch + SHA-verify
#   jq       (fact assembly)          https://jqlang.github.io/jq/
#   cosign   (keyless sign/verify)    pinned; `make bin` fetches it SHA-verified
#   grype    (CVE scan)             pinned; `make bin` fetches it SHA-verified
#   gh       (SLSA attestation verify) https://cli.github.com/
# Optional, for regenerating derived artifacts / the observed carve:
#   sbom-convert (protobom, CDX<->SPDX), uswid (coSWID), FMMT.py (edk2 BaseTools)
