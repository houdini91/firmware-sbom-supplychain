# signing material — NEVER commit
*.key
*.pem
cosign.key
cosign.pub
*.private

# tool binaries fetched locally
bin/opa
bin/cosign
bin/valint

# scratch / generated
*.tmp
.tmp/
tmp/
__pycache__/
*.pyc
venv/
.env

# OS
.DS_Store

# cosign local keys
oss-lane/.keys/

valint-lane/.keys/

# generated per run
inputs/gate-input.json
inputs/grype.json
inputs/sbom.att.bundle
inputs/sbom.slsa.bundle
inputs/slsa-predicate.json
inputs/build-tools.cdx.json
inputs/build-tools.sig.bundle
# per-run in-toto Statements + DSSE attestation bundles (multi-subject: firmware D + file H), signed in CI
inputs/*.intoto.json
inputs/vex.pred.json
inputs/chipsec.pred.json
inputs/sbom.cdx.att.bundle
inputs/vex.att.bundle
inputs/chipsec.att.bundle
inputs/build-tools.att.bundle
inputs/vsa.att.bundle
inputs/byte-integrity.att.bundle
inputs/binary-hardening.att.bundle

# downloaded tool binary (not source)
bin/sbom-convert

# FMMT writes a build log into CWD
FMMT_Build.log
bin/

# private engagement strategy / outreach drafts — keep local, never publish
planning/engagement/
